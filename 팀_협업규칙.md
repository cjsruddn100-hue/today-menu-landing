# 오늘은 뭐먹지 — 팀 랜딩페이지 협업 규칙

## 1. 역할 분담 (7섹션 → 3명)
| 담당 | 섹션 | 작성 파일 | class 접두어 |
|------|------|-----------|--------------|
| A | Header · Hero · Footer | header.css / hero.css / footer.css | `header-*`, `hero-*`, `footer-*` |
| B | Service · Features | service.css / features.css | `service-*`, `features-*` |
| C | Review · FAQ | review.css / faq.css | `review-*`, `faq-*` |

> index.html 뼈대와 common.css는 **공통** — 바꿀 일이 있으면 팀에 먼저 말하기.

## 2. 공통 디자인 (common.css 변수 사용)
- 메인 컬러: `#5B5FEF` → `var(--color-main)`
- 기본 폰트: Pretendard
- 콘텐츠 최대 너비: 1200px → `var(--max-width)` / `.container`
- 버튼 모서리: 8px → `var(--radius)` / `.button`
- 섹션 사이 간격: 80px → `var(--section-gap)`

## 3. 우리 팀 코드 규칙
1. CSS 스타일링은 **class 중심**으로 (id는 꼭 필요할 때만).
2. 개인 영역 class에는 **섹션 이름을 접두어**로 (`features-card`, `review-card`, `faq-item`).
3. 공통으로 쓰는 class는 미리 정함 → `.container`, `.button`, `.section-title`.
4. `h1`, `p`, `button` 같은 **HTML 태그에 직접 CSS 쓰지 않기** (class로 작성).
5. **다른 사람 담당 영역의 class/CSS는 임의로 바꾸지 않기.**

## 4. 폴더 구조
```
머먹팀프로잭트/
├─ index.html          (공통 뼈대)
├─ css/
│  ├─ common.css       (공통)
│  ├─ header.css / hero.css / footer.css   (A)
│  ├─ service.css / features.css           (B)
│  └─ review.css / faq.css                 (C)
└─ assets/             (이미지 등)
```

## 5. 작업 순서
1. (완료) 섹션 나누기 → 공통 규칙 → 뼈대 세팅
2. 각자 자기 섹션 HTML 내용 채우고 자기 css 파일 작성
3. Live Server로 각자 확인 → 합쳐서 전체 점검
